#include <stdio.h> 

int main()
{
	//Eşitlik kontrolü (== , !={Eşit değilse }) 5 == 3 --> False yani 0  ,  5 == 5 --> True yani 1. ,Farklı bir değer girdi a != 5  
	//
	//Büyüklük / Küçülük kontrolü (> , < , <=, >=)



	/*
	İki mantıksal işlemin birleştirilmesi

	Ve bağlacı (&&)
	Veya bağlacı (||)

Example :
	a > 3 || a < 2 
	a <=  3 && a >= 0


	a = a + 5;
	a += 5;
	
	 */
	return 0;
}